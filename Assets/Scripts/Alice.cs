using Unity.Mathematics;
using UnityEngine;

public class Alice : MonoBehaviour {
    private void Start() {
        //quaternion curQuat = new(0, 0, 0, 1);
        //quaternion targetQuat = quaternion.LookRotation(new(0, 0, -1), math.up());
        //quaternion rotateDir = math.normalize(math.mul(
        //    math.inverse(curQuat),
        //    targetQuat));

        //curQuat = math.mul(rotateDir, math.normalize(curQuat));

        //Debug.LogWarning(
        //    curQuat + "\n" +
        //    math.inverse(curQuat) + "\n" +
        //    rotateDir + "\n" +
        //    math.Euler(rotateDir));
    }
}
